{
  "name": "mission-green-plate",
  "version": "1.0.0",
  "description": "Mission Green Plate - School Food Waste Gamification System",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "init-db": "node backend/db/init.js",
    "start": "concurrently \"npm run start --prefix backend\" \"npm run preview --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
