# Leave empty during local development (Vite proxy handles /api → localhost:5000)
# Set this ONLY when deploying frontend separately from the backend
# VITE_API_URL=https://your-backend.railway.app/api
